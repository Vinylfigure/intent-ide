import { EditorState, Transaction } from 'prosemirror-state'
import { EditorView } from 'prosemirror-view'

// No-op for now — custom commands will be added as features are built
export function placeholder() {}
